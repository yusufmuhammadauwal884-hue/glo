
spring.application.name=glovendor_backend_dev

# Allow platform to override the port
server.port=${PORT:8080}

# Datasource - use environment variables for secrets
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://sql8.freesqldatabase.com:3306/sql8801552}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:sql8801552}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:UEaXXEYYfw}

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

#With the below command data will not be delete after system restart
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql= true
spring.jpa.properties.hibernate.format_sql= true
#server.error.include-message=always
#server.error.include-message=always

#spring.jpa.properties.hibernate.dialect= org.hibernate.dialect.MySQLDialect

logging.level.org.springframework.security=DEBUG

# Optional vendor login credentials (set via environment variables on the host)
vendor.login.username=${VENDOR_USERNAME:vendor@example.com}
vendor.login.password=${VENDOR_PASSWORD:changeme}
